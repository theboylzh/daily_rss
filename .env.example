# AI配置
# 必需：AI模型的API密钥（如DeepSeek）
AI_API_KEY=your_ai_api_key_here
# AI模型名称，默认使用DeepSeek
AI_MODEL=deepseek-chat
# AI模型的API地址
AI_API_URL=https://api.deepseek.com/v1/chat/completions

# 邮箱配置
# 必需：发送邮件的邮箱地址
EMAIL_SENDER=your_email@example.com
# 必需：接收邮件的邮箱地址
EMAIL_RECEIVER=recipient@example.com
# 必需：发送邮件的邮箱密码（建议使用应用密码）
EMAIL_PASSWORD=your_email_password_or_app_password
# SMTP服务器地址
EMAIL_SMTP_SERVER=smtp.example.com
# SMTP服务器端口
EMAIL_SMTP_PORT=465

# Tavily配置（可选）
# Tavily搜索的API密钥，用于增强AI分析
TAVILY_API_KEY=your_tavily_api_key_here

# 项目配置
# 项目名称
PROJECT_NAME=Daily RSS

# 订阅源配置（可选）
# 订阅源列表，多个URL用逗号分隔
# SUBSCRIPTIONS=["https://example.com/feed", "https://example.org/rss"]

# 数据存储配置（可选）
# 新闻保留天数
# NEWS_RETENTION_DAYS=60

# 定时任务配置（可选）
# 每日任务执行时间（格式：HH:MM:SS）
# DAILY_SCHEDULE_TIME=08:00:00

# 注意事项：
# 1. 复制此文件为.env并填写实际值
# 2. 不要将.env文件提交到版本控制系统
# 3. 对于GitHub Actions部署，这些值将通过Secrets设置
# 4. 建议使用邮箱的应用密码而不是主密码
# 5. 如何获取邮箱应用密码：
#    - 163邮箱：设置 > POP3/SMTP/IMAP > 客户端授权密码
#    - Gmail：管理您的Google账户 > 安全性 > 应用密码
